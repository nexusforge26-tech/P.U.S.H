"use client";
import {useRouter,useSearchParams} from "next/navigation";import{useState}from"react";
export default function SearchBox({placeholder="ابحث عن مساق، ملخص، امتحان أو فيديو..."}:{placeholder?:string}){const router=useRouter(),sp=useSearchParams(),[value,setValue]=useState(sp.get("q")??"");function submit(e:React.FormEvent){e.preventDefault();const p=new URLSearchParams(sp.toString());value.trim()?p.set("q",value.trim()):p.delete("q");p.delete("page");router.push(`/browse?${p.toString()}`)}return <form onSubmit={submit} className="flex w-full gap-2 rounded-[22px] border border-olive/10 bg-white p-1.5 shadow-soft"><div className="relative flex-1"><svg viewBox="0 0 24 24" className="pointer-events-none absolute right-4 top-1/2 h-5 w-5 -translate-y-1/2 text-olive/45" fill="none" stroke="currentColor" strokeWidth="2"><circle cx="11" cy="11" r="7"/><path d="m20 20-3.5-3.5" strokeLinecap="round"/></svg><input value={value} onChange={e=>setValue(e.target.value)} placeholder={placeholder} className="w-full rounded-2xl bg-transparent py-3.5 pe-12 ps-4 text-sm text-ink placeholder:text-ink/35 focus:outline-none"/></div><button type="submit" className="rounded-[17px] bg-clay px-5 text-sm font-bold text-parchment shadow-card transition hover:bg-clay-dark">بحث</button></form>}
